name: one network activity 2 policy violations
on:
  workflow_dispatch:

permissions:
  checks: write
  contents: write
  packages: read

jobs:
  python-application-build:
    runs-on: ubuntu-latest
    name: Run build with anomalies
    steps:
      - name: Setup PSE
        uses: invisirisk/pse-action@latest
        with:
          api_url: "https://app.stage.invisirisk.com"
          app_token: SZhakDg-w5qUdiAo1oY9q9C7sTI1vs6IMVRmUZwgUq8d9Vb9Y0CjtsQdZIYVphCDHLNZ83_-PzgAVn5SG_f5dw
          
      - uses: actions/checkout@v4

      # Setup Node.js
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22.11.0"

      - name: Install stability buffer packaeg
        run: npm install table-js@10.0.0-esm.0

    
