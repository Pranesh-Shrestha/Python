name: http 8000 port

on:
  workflow_dispatch:

jobs:
  curl-test:
    runs-on: ubuntu-latest
    steps:
      # ============= InvisiRisk Setup =============
      - name: Setup PSE
        uses: invisirisk/pse-action@latest
        with:
          api_url: "https://app.stage.invisirisk.com"
          app_token: nwKOZOENyIrLFvYUKrRwgX4S2ntR_PKxVZHkN7S0fSrWSWH_HAlOPVGaF5wcyqHSkKMWF7U5dMj-rGHXg7AE4g
          mode: "docker-intercept"
      # ============= InvisiRisk Setup End =============
      - name: Curl test (port 8000 + path)
        run: |
          curl -v --max-time 10 http://23.254.164.92:8000 || true
