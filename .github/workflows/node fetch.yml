name: node fetch

on:
  workflow_dispatch:

jobs:
  curl-test:
    runs-on: ubuntu-latest
    steps:
      # ============= InvisiRisk Setup =============
      - name: Setup PSE
        uses: invisirisk/pse-action@latest
        with:
          api_url: "https://app.stage.invisirisk.com"
          app_token: eOCrpmG0BPU8z4ioWFIwcvHLdnQ0Ll-GlcNuowc_A9ZtP2F8_RccXIZo4xMmYNmp8YC-VdVPDhygAToCwLRyFg
          mode: "sidecar"
      # ============= InvisiRisk Setup End =============
      - name: node fetch
        run: |
          node -e "fetch('https://tekvortex.com').then(r => r.text()).then(console.log)"
